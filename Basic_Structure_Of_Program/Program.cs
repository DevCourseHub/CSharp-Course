using System; //It is the library which contains function like "Console.Writeline("");"
using System.Collections.Generic; //they are not using so they are grey out.
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;

namespace Basic_Structure_Of_Program //A namespace is like a container (box) that keeps related classes together.
{
    class Program //A blueprint that contains data and methods.
    {
        static void Main()//A block of code that performs a task.
        {
            Console.WriteLine("Hello World");//It is the instruction.here the ; means that the command ends here.
            Console.ReadLine(); 
                 
        }
    }
}
//by the way the text with '//' is comment.It is not compiled by compiler.It is used to tell people about our program etc.Two Types of comment-
//1 single line comment(//) It ends when line ends
// 2 Multi line comment(/* ---*/) it is extendable to many lines
//eg-
/*This
 * is 
 * multiline
 * comment */