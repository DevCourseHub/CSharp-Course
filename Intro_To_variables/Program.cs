using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Runtime.Remoting.Channels;
using System.Text;
using System.Threading.Tasks;

namespace Intro_To_variables
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //What is variables-
            //The variable is Like a Container Which Holds A value.We do this like below-
            int A = 50; //btw "int" is datatypes (we will discuss it later)
            int B = 100;
            Console.WriteLine(A); //we have printed the A variable.We print the variable without "" quotation marks
            Console.WriteLine("The second number is " + B); //To print variable with the words or string we use +var name after the " mark
            Console.ReadLine();
        }
    }
}
